# Happy Prime Starter Theme

1. Search and replace `translation-slug` with a slug more appropriate for the actual theme or project.
2. Search and replace `$theme_prefix_` with another variable prefix more appropriate for the theme or project.
3. Search and replace `ThemeMainNamespace` with another namespace more appropriate for the theme or project.
4. Search and replace `themeprefix` with another prefix more appropriate for the theme or project.
